import { Text } from "@chakra-ui/react";

export const About = () => {
  return (
    <Text fontSize="md" color="#fff">
      This is the 'About' page, a public route not protected with login.
    </Text>
  );
};
